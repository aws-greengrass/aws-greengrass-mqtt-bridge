services:
    aws.greengrass.clientdevices.Mqtt.Bridge:
        configuration:
            brokerServerUri: 'tcp://localhost:8883'
    main:
        dependencies:
            - aws.greengrass.clientdevices.Mqtt.Bridge