services:
    aws.greengrass.clientdevices.mqtt.Bridge:
        configuration:
            brokerUri: 'tcp://localhost:8883'
            brokerConnectionOptions:
                password: password
    main:
        dependencies:
            - aws.greengrass.clientdevices.mqtt.Bridge
