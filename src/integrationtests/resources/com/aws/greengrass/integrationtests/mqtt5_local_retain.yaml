services:
  aws.greengrass.clientdevices.mqtt.Bridge:
    configuration:
      brokerUri: 'tcp://localhost:8883'
      mqtt5RouteOptions:
        toLocal:
          retainAsPublished: true
      mqttTopicMapping:
        toLocal:
          topic: topic/toLocal
          source: LocalMqtt
          target: LocalMqtt
      brokerClient:
        version: "mqtt5"
  main:
    dependencies:
      - aws.greengrass.clientdevices.mqtt.Bridge
